logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG

spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        hbm2ddl:
          auto: update
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres

security:
  basic:
    enabled: false

jwt:
  private.key: classpath:app.key
  public.key: classpath:app.pub

token:
  access.expiration: 86400 # 24 hours
  refresh.expiration: 7776000 # 90 days
